/**
 * Resource declarations for Showcase Plugin
 *
 * Exports all resource definitions for use throughout the application.
 * Types should be imported directly from the generated types in ../types/
 */

export { job } from './job';
export type { JobListParams } from './job';
