// Fixture placeholder for IR tests
// For CLI config guidance see https://github.com/theGeekist/wp-kernel/blob/main/docs/internal/cli-migration-phases.md#authoring-safety-lint-rules
/**
 * Minimal placeholder WPKernel configuration used by IR unit tests.
 *
 * Tests can import and extend this object to provide a lightweight
 * workspace configuration without needing a full example project.
 *
 * @category IR
 */
export const wpkConfig = {};
