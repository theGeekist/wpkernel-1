/**
 * Immutable dataflow evaluation for WPKernel.
 *
 * The package root is the native Pipeline v2 contract. The serial compatibility
 * adapter and authoring surface remain available only from `@wpkernel/pipeline/v1`.
 *
 * @packageDocumentation
 */

export * from './v2/index.js';
