# AST decisions

Accepted cross-cutting decisions are recorded here when task-local contract
text is insufficient. Files use `ADR-NNN-title.md`, include an explicit
`Status:` line and are named from task `decision_dependencies`.
