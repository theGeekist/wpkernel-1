# Pipeline decisions

Files use `ADR-NNN-title.md` and contain an explicit lowercase `Status:` line.
`accepted` records own semantic boundaries. `proposed` records preserve open choices without
smuggling them into implementation.
