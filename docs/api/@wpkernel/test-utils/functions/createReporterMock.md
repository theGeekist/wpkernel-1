[**@wpkernel/test-utils v0.12.2-beta.0**](../README.md)

---

[@wpkernel/test-utils](../README.md) / createReporterMock

# Function: createReporterMock()

```ts
function createReporterMock(options): ReporterMock;
```

Creates a mock reporter for testing purposes.

## Parameters

### options

[`ReporterMockOptions`](../interfaces/ReporterMockOptions.md) = `{}`

Options for configuring the mock reporter.

## Returns

[`ReporterMock`](../type-aliases/ReporterMock.md)

A `ReporterMock` instance.
