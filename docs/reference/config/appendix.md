<script setup>
import { configFieldIndex } from './fields';
</script>

# Config Appendix

Use this appendix as a browseable reference for `wpk.config.ts`. Each card lists what a field does, what it generates, and how it affects the CLI and runtime.

<ConfigAppendix :fields="configFieldIndex" />
